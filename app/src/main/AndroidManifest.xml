<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="moneyassistant.expert">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".MoneyAssistant"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.activity.SplashActivity"
            android:theme="@style/AppThemeSplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.IntroActivity"/>
        <activity android:name=".view.activity.NotificationActivity" />
        <activity android:name=".view.activity.TransactionActivity" />
        <activity android:name=".view.activity.AddTransactionActivity" />
        <activity android:name=".view.activity.MainActivity" />
        <activity android:name=".view.activity.AccountActivity" />
        <activity android:name=".view.activity.CategoriesActivity" />
        <activity android:name=".view.activity.CategoryActivity" />
        <activity
            android:name=".view.activity.IconsDialog"
            android:excludeFromRecents="true"
            android:theme="@style/AppTheme.Dialog" />
        <activity
            android:name=".view.activity.AccountsDialog"
            android:excludeFromRecents="true"
            android:theme="@style/AppTheme.Dialog" />
        <activity
            android:name=".view.activity.CategoriesDialog"
            android:excludeFromRecents="true"
            android:theme="@style/AppTheme.Dialog" />
        <activity
            android:name=".view.activity.ExportActivity"
            android:excludeFromRecents="true"
            android:theme="@style/AppTheme.Dialog" />

        <service
            android:name=".util.NotificationService"
            android:label="Notify Process"
            android:permission="android.permission.BIND_JOB_SERVICE" />

    </application>

</manifest>